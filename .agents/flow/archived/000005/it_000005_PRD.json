{
  "goals": [
    "Provide a `useDynamicFont` hook that re-measures all rows synchronously when the font changes, with debounced `prepare()` for performance.",
    "Provide a `useExportCanvas` hook that produces a full-table PNG `Blob` from pretext geometry alone.",
    "Provide a `useSearch` hook that returns filtered rows and per-cell match coordinates for highlight rendering.",
    "Expose `whiteSpace: 'pre-wrap'` as a `useMeasure` option so multi-line/tabbed cell content measures correctly.",
    "Deliver a `GridTable` component using CSS Grid with sticky header support, wired to `useMeasure`.",
    "Demo all five additions on the existing demo page."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "useDynamicFont — change font at runtime",
      "description": "As a developer consuming `pretext-tables`, I want to call `setFont('20px Inter')` and immediately receive updated `rowHeights[]` so that my table reshapes without triggering DOM reflows.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "`useDynamicFont(rows, columnWidths, initialFont)` returns `{ rowHeights, setFont, currentFont }`."
        },
        {
          "id": "US-001-AC02",
          "text": "Calling `setFont()` with a new font string triggers `prepare()` (debounced, default 150 ms) + `layout()`."
        },
        {
          "id": "US-001-AC03",
          "text": "`layout()` runs immediately on each call using the previously prepared state, so heights update visually before the debounce fires."
        },
        {
          "id": "US-001-AC04",
          "text": "After the debounce fires `prepare()`, heights are re-computed with the new font and stabilise."
        },
        {
          "id": "US-001-AC05",
          "text": "`useDynamicFont` composes correctly with `useResizable` — `columnWidths` passed to both hooks stay in sync."
        },
        {
          "id": "US-001-AC06",
          "text": "Hook is exported from `src/shared/hooks/index.ts`."
        },
        {
          "id": "US-001-AC07",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "useDynamicFont — demo with size slider and family selector",
      "description": "As a developer evaluating the library, I want to see a live demo where a font-size slider and a font-family selector reshape all row heights in real time so that I can confirm zero DOM cost.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "Demo page includes a `DynamicFontDemo` section with a range slider (12 px – 32 px) and a font-family `<select>`."
        },
        {
          "id": "US-002-AC02",
          "text": "Adjusting the slider continuously updates `rowHeights[]` without triggering `getBoundingClientRect` or any DOM measurement."
        },
        {
          "id": "US-002-AC03",
          "text": "Changing the font family updates heights on the next debounce tick."
        },
        {
          "id": "US-002-AC04",
          "text": "Visually verified in browser."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "useExportCanvas — export full table as PNG",
      "description": "As a developer consuming `pretext-tables`, I want to call `exportCanvas()` and receive a PNG `Blob` of the entire table so that I can save or share a snapshot without rendering to the DOM.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "`useExportCanvas(rows, columnWidths, font, options?)` returns `{ exportCanvas: () => Promise<Blob> }`."
        },
        {
          "id": "US-003-AC02",
          "text": "`exportCanvas()` renders all rows (not just the visible window) to an offscreen `<canvas>`."
        },
        {
          "id": "US-003-AC03",
          "text": "Geometry (column widths, row heights) comes from pretext — no DOM measurement."
        },
        {
          "id": "US-003-AC04",
          "text": "Returns a `Promise<Blob>` with MIME type `image/png`."
        },
        {
          "id": "US-003-AC05",
          "text": "Hook is exported from `src/shared/hooks/index.ts`."
        },
        {
          "id": "US-003-AC06",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-004",
      "title": "useExportCanvas — demo with download button",
      "description": "As a developer evaluating the library, I want a \"Download PNG\" button on the demo page so that I can verify the exported image matches the rendered table.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "Demo page includes a `ExportCanvasDemo` section with a \"Download PNG\" button."
        },
        {
          "id": "US-004-AC02",
          "text": "Clicking the button triggers `exportCanvas()` and downloads the result as `table-export.png`."
        },
        {
          "id": "US-004-AC03",
          "text": "Visually verified in browser: exported image matches the visible table content."
        }
      ]
    },
    {
      "id": "US-005",
      "title": "useSearch — filter rows and return match coordinates",
      "description": "As a developer consuming `pretext-tables`, I want to pass a query string and receive filtered rows plus per-cell match coordinates so that I can draw precise highlight rectangles over matched text.",
      "acceptanceCriteria": [
        {
          "id": "US-005-AC01",
          "text": "`useSearch(rows, columnWidths, font, query)` returns `{ filteredRows, matchCoords }`."
        },
        {
          "id": "US-005-AC02",
          "text": "`filteredRows` contains only rows where at least one cell matches the query (case-insensitive substring)."
        },
        {
          "id": "US-005-AC03",
          "text": "`matchCoords` is an array parallel to `filteredRows` — each entry maps column index → `{ x, y, width, height }[]` for each match within that cell."
        },
        {
          "id": "US-005-AC04",
          "text": "Coordinates come from pretext `layoutWithLines()` — no DOM measurement."
        },
        {
          "id": "US-005-AC05",
          "text": "When `query` is empty, `filteredRows` equals `rows` and `matchCoords` is empty."
        },
        {
          "id": "US-005-AC06",
          "text": "Hook is exported from `src/shared/hooks/index.ts`."
        },
        {
          "id": "US-005-AC07",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-006",
      "title": "useSearch — demo with search input and highlight overlay",
      "description": "As a developer evaluating the library, I want a search input on the demo page that highlights matched text in the table so that I can confirm match coordinates are accurate.",
      "acceptanceCriteria": [
        {
          "id": "US-006-AC01",
          "text": "Demo page includes a `SearchDemo` section with a text input and a table that re-renders on each keystroke."
        },
        {
          "id": "US-006-AC02",
          "text": "Matched characters are overlaid with a highlight rectangle drawn from `matchCoords`."
        },
        {
          "id": "US-006-AC03",
          "text": "No matches shows an empty table body (zero rows)."
        },
        {
          "id": "US-006-AC04",
          "text": "Visually verified in browser."
        }
      ]
    },
    {
      "id": "US-007",
      "title": "useMeasure — whiteSpace pre-wrap option",
      "description": "As a developer consuming `pretext-tables`, I want to pass `{ whiteSpace: 'pre-wrap' }` to `useMeasure` so that cells containing newlines or tab-indented content have their heights computed correctly.",
      "acceptanceCriteria": [
        {
          "id": "US-007-AC01",
          "text": "`useMeasure(rows, columnWidths, { whiteSpace: 'pre-wrap' })` passes the option through to pretext `prepare()` / `layout()`."
        },
        {
          "id": "US-007-AC02",
          "text": "`rowHeights[]` for a cell containing `\"line1\\nline2\"` with `pre-wrap` is taller than the same cell without `pre-wrap`."
        },
        {
          "id": "US-007-AC03",
          "text": "The option defaults to `undefined` (no change to existing behaviour when omitted)."
        },
        {
          "id": "US-007-AC04",
          "text": "`useMeasure` signature and types in `src/shared/hooks/useMeasure.ts` are updated."
        },
        {
          "id": "US-007-AC05",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-008",
      "title": "GridTable — CSS Grid-based table component",
      "description": "As a developer consuming `pretext-tables`, I want a `GridTable` component built with CSS Grid so that I get reliable `overflow: hidden` on cells, working sticky headers, and no `<table>` layout interference.",
      "acceptanceCriteria": [
        {
          "id": "US-008-AC01",
          "text": "`GridTable` renders using `display: grid` divs — no `<table>`, `<tr>`, or `<td>` elements."
        },
        {
          "id": "US-008-AC02",
          "text": "`grid-template-columns` is derived from `columnWidths`."
        },
        {
          "id": "US-008-AC03",
          "text": "`rowHeight` from `useMeasure` is applied as an explicit `height` on each row div."
        },
        {
          "id": "US-008-AC04",
          "text": "The header row is `position: sticky; top: 0` and stays pinned when scrolling."
        },
        {
          "id": "US-008-AC05",
          "text": "`overflow: hidden` on cells clips content that exceeds the measured height."
        },
        {
          "id": "US-008-AC06",
          "text": "Component lives at `src/tables/grid-table/index.tsx` with `measure.ts` and `grid-table.css`."
        },
        {
          "id": "US-008-AC07",
          "text": "Exported from `src/tables/index.ts`."
        },
        {
          "id": "US-008-AC08",
          "text": "Typecheck / lint passes."
        },
        {
          "id": "US-008-AC09",
          "text": "Visually verified in browser."
        }
      ]
    },
    {
      "id": "US-009",
      "title": "GridTable — demo alongside BasicTable",
      "description": "As a developer evaluating the library, I want to see `GridTable` demoed next to `BasicTable` on the demo page so that I can compare the rendering and confirm sticky headers work.",
      "acceptanceCriteria": [
        {
          "id": "US-009-AC01",
          "text": "Demo page includes a `GridTableDemo` section with the same dataset used for `BasicTable`."
        },
        {
          "id": "US-009-AC02",
          "text": "Scrolling within `GridTableDemo` keeps the header row pinned."
        },
        {
          "id": "US-009-AC03",
          "text": "Visually verified in browser."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "`useDynamicFont` must debounce `prepare()` with a configurable delay (`{ debounceMs?: number }`, default 150 ms) and run `layout()` immediately on each font change using the prior prepared state."
    },
    {
      "id": "FR-2",
      "description": "`useDynamicFont` must return `{ rowHeights: number[], setFont: (font: string) => void, currentFont: string }`."
    },
    {
      "id": "FR-3",
      "description": "`useExportCanvas` must render all rows (full dataset, not just virtual window) to an offscreen canvas using only pretext geometry."
    },
    {
      "id": "FR-4",
      "description": "`useExportCanvas` must return `{ exportCanvas: () => Promise<Blob> }` with `image/png` MIME type."
    },
    {
      "id": "FR-5",
      "description": "`useSearch` must return `{ filteredRows: Row[], matchCoords: MatchCoordMap[] }` where coordinates are derived from `layoutWithLines()`."
    },
    {
      "id": "FR-6",
      "description": "`useSearch` must be case-insensitive and treat empty query as \"show all rows\"."
    },
    {
      "id": "FR-7",
      "description": "`useMeasure` must accept an optional `options` parameter `{ whiteSpace?: 'pre-wrap' }` and forward it to pretext."
    },
    {
      "id": "FR-8",
      "description": "`GridTable` must use `display: grid` for all layout — no HTML table elements."
    },
    {
      "id": "FR-9",
      "description": "All new hooks must be exported from `src/shared/hooks/index.ts`; `GridTable` must be exported from `src/tables/index.ts`."
    },
    {
      "id": "FR-10",
      "description": "No font strings may be inlined — all fonts must come from `src/shared/fonts.ts`."
    },
    {
      "id": "FR-11",
      "description": "No DOM measurement (`getBoundingClientRect`, `offsetHeight`, `ResizeObserver` for cell sizing) may be used in any new hook or component."
    }
  ]
}
